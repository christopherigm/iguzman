# `@repo/ui` - Component Library

## Use These First

**Rule:** When writing UI code in any Next.js app in `apps/`, always prefer `@repo/ui` components over raw HTML elements or one-off implementations.

### Mandatory usage rules

| Trigger                                                         | Required component                                | Never do instead                                                                       |
| --------------------------------------------------------------- | ------------------------------------------------- | -------------------------------------------------------------------------------------- |
| Rendering an array of items in a responsive column grid         | `<Grid container>` + `<Grid size={...}>` per item | One-off `display: grid` CSS wrapper in a component file                                |
| Any centered, max-width page section                            | `<Container size="...">`                          | Custom `max-width` + `margin: auto` div                                                |
| Any heading, body copy, label, or caption                       | `<Typography as="h1-h6\|p\|span" variant="...">`  | Bare `<h1>`-`<h6>`, `<p>`, `<span>` for content text                                   |
| Any structural `<div>` wrapper                                  | `<Box>`                                           | Bare `<div>`                                                                           |
| Bordered flex-column content card                               | `<Card>`                                          | Repeating `border`/`padding`/`borderRadius` on `<Box>`                                 |
| Any clickable button or internal-link button                    | `<Button>` or `<LinkButton>`                      | Bare `<button>` or `<a>` for internal navigation                                       |
| A screen's single primary action, floating over content         | `<FloatingActionButton>`                          | A hand-positioned `fixed` `<Button>` with a `zIndex`                                   |
| Grab affordance on a drag-to-reorder row, tile, or list item    | `<MoveHandle>`                                    | A `<span>`/`<div>` with a `⠿` glyph and hand-rolled chip styling                       |
| Any text field or textarea                                      | `<TextInput>` / `<TextInput multirow>`            | Bare `<input>` or `<textarea>`                                                         |
| Any dropdown / option picker                                    | `<Select>`                                        | Bare `<select>`                                                                        |
| Status tag, category label, or metadata chip                    | `<Badge>`                                         | One-off `<span>` with pill/tag styling                                                 |
| Transient success/error notification                            | `<Toast>`                                         | One-off inline `<span>` with success/error color                                       |
| A record's photographs on a detail page                         | `<ImageGallery>`                                  | A per-app slideshow, contact sheet, or one-photo lightbox                              |
| Scrolling the reader anywhere - an element, the top of the page | `scrollToElement` / `scrollToTop` / `<ScrollTo>`  | `el.scrollIntoView({ behavior: 'smooth' })`, `window.scrollTo({ behavior: 'smooth' })` |

### Pre-flight checklist - run this before finishing any `.tsx` file

Grep the file for each of these patterns and replace every hit with the corresponding component:

| If you find…                                                                 | Replace with                                            |
| ---------------------------------------------------------------------------- | ------------------------------------------------------- |
| `<Box` with `border` + `padding` + `borderRadius` + `flexDirection="column"` | `<Card>` - drop the four repeated props                 |
| `<p ` or `<p>`                                                               | `<Typography as="p" variant="...">`                     |
| `<h1>`-`<h6>`                                                                | `<Typography as="hN" variant="hN">`                     |
| `<span` (content text)                                                       | `<Typography as="span" variant="...">`                  |
| `<div` (structural wrapper)                                                  | `<Box>`                                                 |
| `<button`                                                                    | `<Button>` (or `<Button unstyled>` for custom-styled)   |
| `<input`                                                                     | `<TextInput>`                                           |
| `<textarea`                                                                  | `<TextInput multirow>`                                  |
| `<select`                                                                    | `<Select>` - see usage pattern below                    |
| one-off pill/tag `<span>`                                                    | `<Badge>`                                               |
| inline success/error `<span>`                                                | `<Typography>` (inline) or `<Toast>` (transient banner) |
| `.scrollIntoView(` / `window.scrollTo(`                                      | `scrollToElement` / `scrollWindowTo` / `scrollToTop`    |

A `<span>` used purely as a layout/structural container (not rendering text or acting as a tag) does not need to become `<Typography>` or `<Badge>` - use judgment. The trigger is a `<span>` whose primary content is text or a label.

### `<Select>` usage pattern

**Never use a bare `<select>` element.** Always use `<Select>` from `@repo/ui/core-elements/select`. It handles dark-mode theming, floating label, focus ring, and `color-scheme` - no companion CSS needed.

```tsx
import { Select } from "@repo/ui/core-elements/select";

// Build options from a static enum + translation function:
const options = MY_VALUES.map((v) => ({
  value: v,
  label: t(`namespace.${v}`),
}));

// Render - the label prop replaces any manual <Typography> label above the field:
<Select
  label={t("myFieldLabel")}
  value={currentValue}
  onChange={(v) => setCurrentValue(v as MyType)}
  options={options}
/>;
```

**Never** wrap it in a custom `Box`+`Typography` label pair - pass `label` directly to `<Select>` instead. **Never** write a `.my-select { background: ... }` CSS class - the component already applies `--surface-2`, `--foreground`, and `color-scheme: light dark`.

**When the list is long** (a whole catalog of places, every species, a country list), pass those same options to **`<TextInput options={...}>`** instead - it is the searchable form of this control, with the same `value`/`onChange` contract, so switching between them is one prop. Don't hand-roll a filter box above a `<Select>`, and don't reach for it for a ten-item list: a short `<Select>` gets the phone's native picker, which is better than anything a dropdown of ours can do.

### Typography Scale - Size Reference

The `Typography` component defines the only approved font-size scale. **Never hardcode a pixel font-size on a `<Typography>` component.** Pick the variant whose native size matches your intent, then drop the `styles={{ fontSize }}` override entirely.

| Variant   | Rendered size        | Use for                                |
| --------- | -------------------- | -------------------------------------- |
| `label`   | 0.75 rem (12 px)     | Field labels, metadata chips, tag text |
| `caption` | 0.8125 rem (13 px)   | Secondary body copy, hints, timestamps |
| `h6`      | 0.875 rem (14 px)    | Compact card body / paragraph headings |
| `body`    | 1 rem (16 px)        | Default prose **and small body copy**  |
| `h5`      | clamp(13 px → 15 px) | Compact card names                     |
| `h4`      | clamp(15 px → 18 px) | Secondary headings                     |
| `h3`      | clamp(17 px → 22 px) | Feature card names                     |
| `h2`      | clamp(22 px → 32 px) | Section titles                         |
| `h1`      | clamp(32 px → 56 px) | Hero headings                          |

#### `body-sm` is removed - do not use it

The `body-sm` variant **no longer exists**. It was overused as a default and made body copy cramped and hard to read. Rules:

- **Default to `body`** for all body copy, including secondary text, hints, error/success messages, card descriptions, and form helper text. When in doubt, `body` is the answer - not a smaller variant.
- **Use `caption` only when it is strictly necessary** - i.e. genuinely de-emphasised metadata where the smaller size carries meaning: timestamps, byline/source attributions, dense tabular hints, and inline counts. Never reach for `caption` just to make text "fit"; resize the container instead.
- **Use `label` (12 px)** for field labels and tag/chip text only.
- There is **no 14 px body variant**. If a compact 14 px paragraph is unavoidable, `h6` is the only 14 px slot - but prefer `body`.

**The only valid reason to keep `styles={{ fontSize }}` on a `<Typography>` is when no variant matches and a specific sub-scale size (e.g. 11 px) is required.** In that case, choose the closest variant (`label` for anything ≤ 12 px) and add the override - but leave a brief inline comment so the next reader understands why.

**For native form elements** (`<select>`, `<input>`, `<textarea>`, bare `<button>`) where `<Typography>` cannot be used, font-size is acceptable in CSS but **must use rem values from the scale above** - never raw px values:

| px equivalent | rem to write |
| ------------- | ------------ |
| 12 px         | `0.75rem`    |
| 13 px         | `0.8125rem`  |
| 14 px         | `0.875rem`   |
| 16 px         | `1rem`       |

### `UIComponentProps` - shared layout props (all core-elements accept these)

Most components extend `UIComponentProps`, which maps common CSS properties directly to props:
`display`, `flexDirection`, `justifyContent`, `alignItems`, `flexWrap`, `flexGrow`, `gap`, `flex`, `alignSelf`, `order`, `width`, `height`, `minWidth`, `maxWidth`, `minHeight`, `maxHeight`, `padding`, `paddingTop`, `paddingBottom`, `paddingLeft`, `paddingRight`, `paddingX` (left+right shorthand), `paddingY` (top+bottom shorthand), `margin`, `marginTop`, `marginBottom`, `marginLeft`, `marginRight`, `marginInlineStart`, `marginInlineEnd`, `border`, `borderRadius`, `color`, `backgroundColor`, `shadow` (bool), `elevation` (number), `styles` (CSSProperties override), `className`, `id`, `children`. Individual side props take precedence over `paddingX`/`paddingY`.

> **`Box` auto-applies `display: "flex"`.** Setting any flex _container_ prop on a `Box` - `flexDirection`, `justifyContent`, `alignItems`, `flexWrap`, or `gap` - automatically sets `display: "flex"`, so you do **not** need to pass `display="flex"` alongside them. Pass `display` explicitly only to opt out (`display="grid"`, `display="block"`, etc.); an explicit `display` always wins. This inference is `Box`-only: flex _child_ props (`flex`, `flexGrow`, `alignSelf`, `order`) never trigger it, and other components (`Grid`, `Card`) manage their own `display`.

### Core Elements (`packages/ui/src/core-elements/`)

| Use case                                | Component               | Key props                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------------------------------------- | ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Headings, body text, labels             | `Typography`            | `variant?: 'none'\|'h1'…'h6'\|'body'\|'caption'\|'label'` (no `body-sm` - removed; default to `body`, use `caption` only when strictly necessary), `as?` (HTML element override), `textAlign?`, `fontWeight?`, `role?`, `aria-current?`, `aria-hidden?`, `aria-label?`, `title?` + UIComponentProps. Heading variants h1-h5 use fluid `clamp()` sizing (h1 56px→32px hero, h2 32px→22px section titles, h3 22px→17px feature card names, h4 18px→15px secondary, h5 15px→13px compact card names). Use `variant="none"` only when a CSS class must fully control typography (e.g. `.section-subtitle` with its fluid `clamp()` font-size). Text is always a solid color - never dim it with `opacity` or a semi-transparent color.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Bordered flex-column card               | `Card`                  | Extends `BoxProps` - defaults: `display="flex"`, `flexDirection="column"`, `padding={14}`, `borderRadius={10}`, `border="1px solid var(--border)"`, `elevation={5}`. All props override defaults (pass `href` to make the whole card a locale-aware anchor - locale-less, see `Box`; `padding={0}`/`border="none"` for a full-bleed image card). Import: `@repo/ui/core-elements/card`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Generic `<div>` wrapper                 | `Box`                   | `role?`, `aria-label?`, `aria-hidden?`, `aria-labelledby?`, `aria-describedby?`, `aria-modal?`, `tabIndex?`, `onClick?`, `onKeyDown?`, `onAnimationEnd?`, `onDragOver?`, `onDragEnter?`, `onDragLeave?`, `onDrop?`, `onDragStart?`, `onDragEnd?`, `draggable?` (default `false`; makes the Box a native HTML5 drag **source** so `onDragStart` fires on it - the drag handlers alone only ever receive a drag something else started, which is why a drag-to-reorder grid used to need a bare `<div>`. Applied to the `div` branch only, not the `href` anchor), `translucent?`, `href?` (when set, renders a **locale-aware** anchor - `Link` from `@repo/i18n/navigation` - instead of a `div`, keeping every layout prop; use for a whole card/surface that is a navigation target. ⚠ **Write it locale-less**: `/species/deer`, never `` `/${locale}/species/deer` `` - next-intl prefixes the locale being rendered, which is what makes a stale locale in a link impossible. An absolute/external href passes through untouched), `target?` (`'_blank'` auto-adds `rel="noopener noreferrer"`), `prefetch?` (link prefetch when `href` set) + all `UIComponentProps`. Auto-applies `display: "flex"` when any flex container prop (`flexDirection`, `justifyContent`, `alignItems`, `flexWrap`, `gap`) is set and `display` is not - no need to pass `display="flex"` with them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Centered max-width section              | `Container`             | `size?: 'xs'\|'sm'\|'md'\|'lg'\|'xl'`, `paddingX?`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 12-col responsive layout                | `Grid`                  | `container?`, `item?`, `size?: {xs,sm,md,lg,xl}` (1-12), `hidden?: {xs,sm,md,lg,xl}` (per-breakpoint visibility - each `true` sets `display:none` **within that breakpoint's band only**, range-scoped not cascading: `{xs:true}` hides below sm/shows sm+, `{md:true}` hides only md→lg; compose bands to hide across a range. Thresholds live once in `grid.css` - no consumer writes a media query. Also exports the `GridHidden` type), `reorder?: {xs,sm,md,lg,xl}` of `'first'\|'last'` (per-breakpoint flow order, range-scoped per band like `hidden`: `{xs:'last'}` pushes the item to the end of the flex container below sm - `order:1` - and restores its authored position `order:0` from sm up; `'first'` uses `order:-1`. Thresholds live once in `grid.css`; no effect in masonry. Also exports `GridOrder`, `GridReorder` types), `masonry?: boolean \| {xs,sm,md,lg,xl}` (responsive column count; `true` = `{xs:1,sm:2}`; implies `container`, children flow into balanced CSS multi-column columns with no vertical gaps and ignore their `size`), `spacing?`, `spacingX?`, `spacingY?`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Clickable button                        | `Button`                | `text?` (or `children`), `href?`, `target?` (anchor target e.g. `'_blank'`/`'_self'`, applied to the locale-aware `Link` when `href` is set; `'_blank'` auto-adds `rel="noopener noreferrer"`), `onClick?`, `type?`, `size?: 'sm'\|'md'\|'lg'\|'xl'` (default `'sm'`; sm=6px pad/13px, md=8px 14px/13px, lg=10px 20px/14px+r8, xl=14px pad/16px/r10 - a touch-target size for finger-driven UIs such as the POS till), `onHover?`, `unstyled?` (strips default styles/wave - use for icon or toggle buttons with custom CSS), `disabled?`, `title?`, `icon?` (SVG path - renders an internal `Icon`; auto-sizes to sm→13px/md→15px/lg→17px and adds flex layout automatically), `iconPosition?: 'start'\|'end'` (default `'start'`), `iconSize?` (CSS string override), `iconColor?` (defaults to `currentColor` - inherits button text color), `translucent?` (default `false`; when `true` applies `backdrop-filter: blur(8px)` on the default kind for translucent surfaces - leave off to avoid scroll jank), `aria-label?`, `aria-pressed?`, `aria-expanded?`, `aria-controls?` + UIComponentProps                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Icon-only ghost button                  | `IconButton`            | `icon` (required, SVG path), `aria-label` (required), `href?`, `target?` (applied to the locale-aware `Link` when `href` set; `'_blank'` auto-adds `rel="noopener noreferrer"`), `onClick?`, `type?`, `size?: 'sm'\|'md'\|'lg'` (default `'md'`; square box 28/36/44 px, auto icon 16/18/20 px), `kind?: 'default'\|'primary'\|'error'\|'success'\|'warning'` (default `'default'` - drives icon color + hover tint; `'primary'` uses `--accent`), `iconSize?`, `iconColor?` (defaults to color for `kind`), `fullColor?` (default `false`; passed to the inner `Icon` to render the SVG as a real multi-color image instead of a single-color mask), `solid?` (default `false`; when `true` renders a fully opaque kind-tinted background with a white icon for a high-emphasis filled look - the white icon overrides `iconColor`, only `fullColor` keeps the icon's own colors, and `translucent` blur is suppressed), `disabled?`, `title?`, `isLoading?` (shows Spinner), `translucent?` (default `false`; when `true` applies `backdrop-filter: blur(8px)` for translucent surfaces - leave off to avoid scroll jank), `aria-pressed?`, `aria-expanded?`, `aria-controls?` + UIComponentProps. Transparent by default with a ghost hover tint; renders a native `button` or a locale-aware `Link` (when `href` set and no `onClick`; the href is locale-less, see `Box`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Drag-to-reorder grab handle             | `MoveHandle`            | `aria-label` (**required unless `decorative`** - the glyph carries no text), `title?`, `decorative?` (default `false`; when `true` something else - the whole row, the whole tile - is the drag source, so the handle takes no drag handlers and is `aria-hidden`), `draggable?` (default `true` unless `decorative` - makes the handle itself the native HTML5 drag source), `onDragStart?`, `onDragEnd?`, `size?: 'sm'\|'md'\|'lg'` (default `'md'`; the same 28/36/44 px box and 16/18/20 px glyph as `IconButton`, so a handle sits in a row of icon buttons without reflowing it), `variant?: 'outlined'\|'overlay'` (default `'outlined'` - a faint tinted chip with a muted glyph for a form/table surface; `'overlay'` is a translucent-black chip with a white glyph, the only thing legible over an arbitrary photograph), `icon?` (default `/icons/handler.svg` - the dot-grid glyph **every app must keep at that path in `public/icons/`**; also exported as `MOVE_HANDLE_ICON`), `iconSize?`, `iconColor?` + UIComponentProps (so `height={40}` lines a handle up with a `Select` beside it). ⚠ **It renders a `<span>`, never a `<button>`** - Firefox ignores `draggable` on form controls, so a `<button draggable>` never fires `dragstart` and sort mode would silently die in that browser. The cost is that it is not keyboard-operable, which is why every consumer keeps a non-drag way to the same result (a "use as cover" button, `sort_order` persisted on save); don't "upgrade" it to a `<button>` or add `role="button"`, which would promise an Enter/Space action it does not have. Also exports `MoveHandleSize`, `MoveHandleVariant`, `MoveHandleProps`. Import: `@repo/ui/core-elements/move-handle`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Slider pagination row                   | `SliderDots`            | **The one pagination row every slider in the monorepo uses** - do not hand-roll dots, and do not fall back to Swiper's own `Pagination` module (its bullets render at Swiper's stock blue unless every consumer remembers to override `--swiper-pagination-*`). Two exports. `SliderDots` is the dot row: `count` (dots to draw), `active` (current index), `onSelect(index)`, `label` (names the row for a screen reader), `dotLabel?: (index) => string` (**pass it whenever the slides have titles** - "Fawn in the north meadow" beats "3"; defaults to the 1-based position) + UIComponentProps. `SliderControls` wraps it in the full row - the two arrows flanking the dots, **beneath** the slide rather than floating over it - and adds `onPrev`, `onNext`, `previousLabel`, `nextLabel`, `previousIcon?`/`nextIcon?` (default `/icons/prev.svg`, `/icons/next.svg` - the pair **every app must keep in `public/icons/`**), `arrowKind?: IconButtonKind` (default `'success'`), `arrowSize?: 'sm'\|'md'\|'lg'` (default `'md'`), `translucentArrows?` (default `true`; turn it **off** inside an opaque card, where there is nothing behind the arrows worth blurring). Both dot states are painted from `--accent`: the resting dots are that hue mixed to a tint, the current one is a solid 26 px **pill** - a pill, not a bigger dot, so the row keeps its height and nothing below it shifts on a slide change. `SliderControls` renders nothing for `count <= 1`. ⚠ It is **slider-agnostic** and knows nothing about Swiper: a looping single-view slider passes `realIndex`/`slideToLoop`, a multi-view one passes `snapIndex`/`snapGrid.length`/`slideTo` - which is what keeps the last dots reachable when several slides share the viewport (a dot per slide would leave them dead). Consumers: `apps/animals`' journal slider, `apps/website`'s success stories, `apps/video-downloader`'s feature slider, `apps/edge-folio`'s company intel. Also exports `SliderDotsProps`, `SliderControlsProps`, `SLIDER_PREV_ICON`, `SLIDER_NEXT_ICON`. Import: `@repo/ui/core-elements/slider-dots`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| A record's photographs, with fullscreen | `ImageGallery`          | **The one photo gallery every detail page in the monorepo uses** - `apps/animals`' three catalog routes (category, species, sighting) and `apps/website`'s five (product, service, menu item, blog post, highlight), through a thin per-app wrapper each that supplies only the strings. Don't hand-roll a second one: this replaced two copies that had already drifted - only one zoomed, only one locked the page behind the overlay, and their fullscreen viewers were different objects. `images: GalleryImage[]` (each `{url, alt, fit?, backgroundColor?}`; `fit` is an `object-fit` value defaulting to `cover`, and `backgroundColor` is what shows around a `contain` one - an illustration or a range map must not be cropped to fill the frame), `placeholderColor?` (behind an empty gallery and behind a `contain` photo), `labels?: ImageGalleryLabels` (`expand`, `previous`, `next`, `close`, `zoomIn`, `zoomOut`, `pagination` - **all default to English**, like `ConfirmationModal`, so a localised app passes its own `Gallery` namespace), `className?`. Renders a `Card` holding one large slide, a 5-up thumbnail strip, and a fullscreen overlay. ⚠ **Fullscreen is a second Swiper, not a lightbox over one photo**: it opens on the slide that was pressed and pages through the whole set with a swipe, carrying no frame and no card - the photograph alone over the scrim, with `SliderControls` beneath it (the row the slide's height budget is reserved for, `--ui-gallery-fs-controls`). ⚠ **It zooms** through Swiper's `Zoom` module - pinch/double-tap, plus the magnifier in the control row, since Swiper's zoom has no wheel handler and a desktop reader would otherwise have only the double-click - and **both halves of the gesture have to be taken from the page** for a pinch to work at all: `useScrollLock` while the overlay is open _and_ `touch-action: none` on the overlay itself, since every app's `globals.css` puts `touch-action: pan-x pan-y` on `<body>` and the browser would answer two fingers by panning the document. ⚠ **The frame is the part to leave alone**: the slide is capped to an Instagram-style 4:5 or 5:4 box chosen from the _most portrait_ photo in the set (measured with `@repo/helpers`' `getImageDimensionsFromUrl`), so a column of pictures of different shapes occupies one stable rectangle instead of resizing the row on every slide change. There is deliberately **no per-breakpoint frame override** - `apps/website`'s old `forceOrientation` was dropped with the move. ⚠ **It reads six icons from the consuming app's `public/icons/`** - `fullscreen`, `prev`, `next`, `close`, `zoom-in`, `zoom-out` - so an app adopting it must carry all six. Also exports `GalleryImage`, `ImageGalleryLabels`, `ImageGalleryProps`. Import: `@repo/ui/core-elements/image-gallery`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Screen's primary floating action        | `FloatingActionButton`  | `icon` (required, SVG path), `aria-label` (required), `label?` (**passing it switches to the extended variant**: a pill with the icon and a short verb, instead of a circle), `href?` (renders a locale-aware `Link` when given without `onClick`; write it **locale-less**, see `Box`), `target?`, `onClick?` (takes precedence over `href`), `type?`, `position?: 'bottom-right'\|'bottom-left'` (which corner it is fixed to, default `'bottom-right'`), `size?: 'md'\|'lg'` (circle diameter 36/42 px, auto icon 16/22 px, default `'lg'`), `kind?: 'primary'\|'default'\|'error'\|'success'\|'warning'` (default `'primary'` - the theme accent; the fill is always opaque, since a FAB is by definition high-emphasis), `offset?` (distance from both screen edges, default `var(--ui-fab-offset, 20px)` so an app can move every FAB from its `globals.css`; the component's own CSS tightens it to 16 px below `sm`), `iconSize?`, `iconColor?` (defaults to white, for contrast on the fill), `fullColor?`, `disabled?`, `isLoading?` (spinner in place of the icon), `title?` (defaults to `aria-label`), `zIndex?` (default `900` - above page content and the fixed navbar, below `Toast` (1000) and `ConfirmationModal` (1100), so a dialog the FAB opens is never drawn under it), `aria-expanded?`, `aria-controls?` + UIComponentProps. **`position: fixed`**, so it does not scroll with the page, takes part in no parent's layout, and may be rendered anywhere in the tree - pair it with `PageBottomSpacer`, or the page's last line sits under it. The bottom offset adds `env(safe-area-inset-bottom)` so it clears an iPhone's home indicator. Import: `@repo/ui/core-elements/floating-action-button`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Text/underline link                     | `LinkButton`            | `label` (required), `href?`, `onClick?`, `aria-label?`, `aria-current?`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Text field / textarea                   | `TextInput`             | `label?`, `value?`, `onChange(v: string)?`, `type?`, `format?: 'text'\|'currency'\|'number'\|'date'\|'phone'` (input mask, default `'text'`; masks the displayed value while `onChange` emits the raw value - currency/number US `$1,234.5`, date `MM/DD/YYYY`, phone `(XXX) XXX-XXXX`; also exports the `TextInputFormat` type), `multirow?`, `rows?`, `placeholder?`, `error?: boolean \| string` (invalid state - a string renders as the field message and supersedes `helperText`; either form sets `aria-invalid` and paints the underline/label/bar with `--error`), `helperText?: string` (hint beneath the field), `options?: TextInputOption[]` (**makes the field a searchable select** - a combobox: the reader types to filter and picks from a dropdown list, and `value`/`onChange` then speak in option _values_ exactly as `Select`'s do while the field displays the matching option's `label`. Matching is case- **and accent-insensitive** across the whole label, so `cienaga` finds `Ciénaga`; an untouched field lists everything, typing narrows it. Focus selects the label so the first keystroke replaces it, ↑/↓ + Enter pick without the mouse (Enter is `preventDefault`ed, so it can never submit an enclosing `<form>`), Escape closes, blur **discards** text that matched nothing, and emptying the field is how a selection is cleared (`onChange('')`). Reach for it over `Select` when the list is long enough to scroll past what you want; `Select` stays the better control for a short one, where a phone's native picker wins. Mutually exclusive with `format`/`type`, both ignored while options are present. Also exports the `TextInputOption` type - structurally identical to `SelectOption`, so one options array serves both), `noOptionsLabel?: string` (row shown when nothing matches; **defaults to English**, so a localised app passes its own - as with `ConfirmationModal`), `onKeyDown?`, `min?`, `max?`, `step?`, `aria-label?` (used when `label` is omitted), `ref?` + UIComponentProps. Types `date`/`time`/`datetime-local`/`week`/`month`/`color` always keep the label floated (browser-native UI occupies the field area). All `NativeInputProps` not listed explicitly are also accepted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Dropdown / option picker                | `Select`                | `options: SelectOption[]` (array of `{value, label}`), `value?`, `onChange(v: string)?`, `label?` (floating label), `disabled?`, `required?` + UIComponentProps. Also exports `SelectOption` type.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Toggle / checkbox                       | `Switch`                | `checked?`, `defaultChecked?`, `onChange(checked: boolean)?`, `disabled?` (default `false`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Status tag / label                      | `Badge`                 | `children`, `variant?: 'filled'\|'outlined'\|'subtle'`, `size?: 'sm'\|'md'\|'lg'`, `color?`, `textColor?`, `uppercase?` (default `false` - renders the label uppercase), `circular?` (default `false`; trims the side padding and floors the width at the badge's own height, so a one/two-character label - a count, a step number - renders as a disc while longer content stretches back into the usual pill), `translucent?` (default `false`; when `true` applies `backdrop-filter: blur(8px)` for badges floating over translucent surfaces - leave off to avoid scroll jank)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Scannable Code 128 barcode              | `Barcode`               | `value` (string to encode, required), `height?: 'sm'\|'md'\|'lg'` (bar height 40/64/100 px, default `'md'`), `color?` (bar + text color, default `'#000000'`), `background?` (default `'#ffffff'` - needed for scannability), `showText?` (human-readable value beneath bars, default `true`), `className?`, `style?`. Self-contained Code 128-B encoder (no dependency); bars stretch to fill the container width. Import: `@repo/ui/core-elements/barcode`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| SVG icon                                | `Icon`                  | `icon` (SVG path, required), `color?`, `size?`, `backgroundColor?`, `backgroundShape?: 'circle'\|'square'\|'triangle'`, `shadow?`, `fullColor?` (default `false`; when `true` renders the SVG as a real multi-color `next/Image` with `objectFit:contain` inside the square `size` box instead of a single-color mask - use for multi-color art/badges that must keep their own colors)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Top navigation bar                      | `Navbar`                | `logo` (required), `items?`, `fixedItems?`, `searchBox?`, `onSearch?`, `translucent?`, `themeSwitch?`, `searchValue?` (controlled search text), `rightSlot?` (ReactNode rendered beside search), `hiddenPaths?` (string[] - exact paths where the navbar and drawer are not rendered). Each `items`/`fixedItems` entry is a `MenuItem`: `label` (pass `""` for an icon-only item), `href?`, `onClick?`, `icon?`, `children?`, plus `ariaLabel?` (**required when `label` is `""`** - the `Icon` is `aria-hidden`, so an icon-only item is otherwise unnamed; it is also the item's `title` tooltip) and `badge?: number \| string` (a small count chip - inline after the label, or over the icon's top-right corner when the item is icon-only; `0`/`""`/absent renders nothing and a number past 99 shows as `99+`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Navbar height spacer                    | `NavbarSpacer`          | No props. Renders a `div` with `height: var(--ui-navbar-height)` (57 px). Place at the top of any page that does not start with a `<Hero>` to push content below the fixed navbar. For full-viewport containers using `styles={{ paddingTop }}`, use `paddingTop: 'var(--ui-navbar-height)'` instead. Both import from `@repo/ui/core-elements/navbar`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Page bottom spacer                      | `PageBottomSpacer`      | No props. Renders a `div` with `height: var(--ui-page-bottom-spacing)` (default 64 px). Place as the last element on any detail or content page to add breathing room below the main content. Override the height via the CSS variable. Imports from `@repo/ui/core-elements/navbar`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Mobile slide-in menu                    | `Drawer`                | `open`, `onClose`, `items`, `logo`, `themeSwitch?`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Loading / progress                      | `ProgressBar`           | `value?` (0-100, omit for indeterminate), `size?` (px height), `label?`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Small / inline loading indicator        | `Spinner`               | `size?` (diameter px, default 20), `thickness?` (border px, default 2), `label?`. Use for non-full-width loading states such as metadata fetches, button pending states, and inline async actions - anywhere `ProgressBar` would be too heavy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Confirm dialog                          | `ConfirmationModal`     | `title`, `text`, `okCallback`, `cancelCallback?`, `okLabel?` (confirm button text, default `'OK'`), `cancelLabel?` (cancel button text, default `'Cancel'`), `children?` (rendered between text and actions), `panelMaxWidth?` (CSS string, default `420px`), `position?: ModalPosition` (`'top'` \| `'center'` \| `'bottom'`, default `'center'` - always horizontally centered), `backgroundBlur?` (CSS `backdrop-filter` value, default `'blur(2px)'`), `okDisabled?`. **The button labels default to English** - this package is i18n-agnostic, so a localised app must pass `okLabel`/`cancelLabel` (e.g. from the shared `Common` namespace: `tCommon('ok')` / `tCommon('cancel')`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Discrete range select                   | `Slider`                | `steps: SliderStep[]` (array of `{value, label}`), `value` (current step value), `onChange(value)`, `label?` (shown above track), `disabled?` + UIComponentProps. Tick labels are clickable; the active step is highlighted in accent color. Also exports `SliderStep` type.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Page breadcrumb trail                   | `Breadcrumbs`           | `items: BreadcrumbItem[]` - each item has `label: string` and optional `href?: string`; items without `href` render as the current page (aria-current). Also exports `BreadcrumbItem` type.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| CSS gradient picker                     | `GradientBuilder`       | `value` (CSS string), `onChange(css: string)`, `label?` (field label above preview), `labels?: GradientBuilderLabels` (i18n overrides - keys: `linear`, `radial`, `solid`, `angle`, `color`, `stops`, `addStop`, `removeStop`, `pickColor`, `opacity`, `rawCss`; all fall back to English defaults). Renders a `Card` containing a live preview strip (over a checkerboard, so transparency reads as transparency), Linear/Radial/Solid type tabs, angle slider (linear), color-stop editor with swatch + hex + position %, and a raw CSS text field. **Each stop supports alpha**: the native swatch is RGB-only, so every stop gets its own 0-100% opacity slider, emitting 8-digit hex (`#rrggbbaa`) or `rgba()` depending on the stop's existing notation; the slider is disabled for notations the component can't rewrite (named colors, `hsl()`). Parses and writes standard CSS gradient strings. Also exports `GradientType`, `ColorStop`, `GradientBuilderLabels` types.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Language/locale picker                  | `LocaleSwitcher`        | `locales: readonly string[]` (all available locale codes), `currentLocale: string` (active locale). Optional: `flags?: Record<string, string>` (emoji flag overrides by locale, defaults to 🇺🇸/🇪🇸/🇫🇷/🇩🇪/🇧🇷), `labels?: Record<string, string>` (display label overrides, defaults to uppercase locale code). Renders a dropdown that opens upward; on selection navigates to the same path under the chosen locale. ⚠ **Goes through `usePathname`/`useRouter` from `@repo/i18n/navigation`, never `next/navigation`** - `router.replace(pathname, { locale })`. It used to rewrite path segment 1 by hand, which navigated fine but skipped next-intl's `syncLocaleCookie`; the middleware does not cover for that (it ignores any request whose `Sec-Fetch-Dest` is not `document`, and a soft navigation's RSC fetch never is), so the URL became `/es` while `NEXT_LOCALE` stayed `en` and every locale-less link on the page bounced the reader back to `/en`. See `apps/CLAUDE.md` → "Link Convention". Suitable for footers and navbars.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Markdown / rich-text body               | `RichText`              | `children` (markdown string, required), `className?`. Renders markdown via `react-markdown` + `remark-gfm` (GFM tables, strikethrough, etc.). Does **not** extend `UIComponentProps` - wrap in `<Box>` for layout props.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Code block with copy button             | `CodeBlock`             | `code` (string, required), `language?` (label shown in header bar, e.g. `'tsx'`), `showCopy?` (default `true` - renders copy-to-clipboard button using inline copy SVG; switches to a checkmark for 2 s after copy). Header bar is omitted entirely when both `language` is absent and `showCopy` is `false`. + UIComponentProps                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Fixed-position notification             | `Toast`                 | `message` (required), `variant: 'error'\|'success'`, `position?: 'bottom-left'\|'bottom-center'\|'bottom-right'\|'top-left'\|'top-center'\|'top-right'` (default `'bottom-left'`), `duration?` (seconds before auto-dismiss, default `5`, pass `0` to never dismiss). Renders fixed on screen with `z-index: 1000`; slides in/out from the nearest edge. Click to dismiss early. Also exports `ToastVariant` and `ToastPosition` types.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Share / copy-link button                | `ShareButton`           | `title` (required - the share sheet's title; the tooltip comes from `label`), `text?` (share sheet blurb, falls back to `title`), `label` (required - accessible label / tooltip), `copiedLabel` (required - toast message shown when the URL was copied instead of shared), `url?` (defaults to the current page URL), `icon?` (default `'/icons/share.svg'`), `kind?` (default `'success'`), `stopPropagation?` (default `false`; stops the click reaching an enclosing link - set when the button sits inside a card that is itself a link, and pass `url` too, since the default target would be the grid rather than the item) + every other `IconButtonProps` (`size`, `disabled`, `translucent`, …) forwarded to the inner `IconButton`. Prefers `navigator.share` (mobile native sheet); where unavailable copies the URL to the clipboard and shows a `Toast`. A user-cancelled share is a no-op. Import: `@repo/ui/core-elements/share-button`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Streaming LLM "enhance" preview         | `StreamingEnhancePanel` | Imperative `ref` handle (`StreamingEnhanceHandle.start(messages: LlmMessage[])`), `onAccept(text)`, `onGeneratingChange?(bool)`, `onPreviewActiveChange?(bool)`, `labels: {stop, discard, accept}` (caller-supplied i18n), `groqOptions?` (forwarded to `useGroqProxy`, default `{temperature: 0.7}`), `className?`. Owns the streaming hook internally so per-token `streamingText` updates re-render only this panel, not the parent form/page. The caller builds the prompt and calls `ref.current.start(messages)`; the panel renders the preview plus Stop (while generating) / Discard + Accept (when done), aborts an in-flight stream on unmount, and returns `null` until a preview exists. Also exports `StreamingEnhanceHandle`, `StreamingEnhanceLabels`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Typeset math formula                    | `MathFormula`           | `formula` (required string - plain-text notation with Unicode math symbols: `×`, `−`, `≥`, `≤`, Greek letters `δ`, `Σ`, etc., and `^(expr)` for superscripts), `displayMode?` (bool, default `false` - block-centered rendering vs inline). Converts the notation to LaTeX internally and renders via KaTeX. Import: `@repo/ui/core-elements/math-formula`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Contact / "ask a question" form         | `ContactForm`           | `labels: ContactFormLabels` (all strings - i18n-agnostic like `ConfirmationModal`), `onSubmit(values) => Promise<void>` (app owns transport; reject → error, resolve → clears message + shows success), `account?: {name,email} \| null` (when set, name/email render as read-only text and only the message is editable - the logged-in case), `heading?`, `description?`, `contextLabel?` (e.g. the item name when embedded on a detail page), `className?`. Manages its own field state + submit lifecycle. Also exports `ContactFormLabels`, `ContactFormAccount`, `ContactFormValues`. Import: `@repo/ui/core-elements/contact-form`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Interactive map, one pin or many        | `OsmMap`                | `markers: OsmMapMarker[]` (each `{id, latitude, longitude, title, icon?, color?}` - `icon` is the glyph drawn in the pin's head, `color` its fill when there is no glyph), `labels: OsmMapLabels` (all strings - i18n-agnostic like `ConfirmationModal`: `map`, `zoomIn`, `zoomOut`, `attribution`, `zoomHint`, `zoomHintMac`, `empty?`, plus `yourLocation?`, `locate?`, `enterFullscreen?`, `exitFullscreen?` - the last four default to English and are only rendered by the controls below), `locateControl?` (default `false`; renders a **button** in the top-right row that pins where the visitor is - the same teardrop as a record's, in `--accent` with a white dot in its head, non-interactive. ⚠ **Nothing asks for the geolocation permission until that button is pressed** - this map used to locate on mount, which put a browser dialog in front of anyone who merely scrolled onto a page carrying a map. A refusal, no fix, or an insecure origin all leave the map exactly as it was, silently - never write a branch that reports the failure. A _press_ **does** centre the camera on the reader (pulling in to zoom 13 if the map was framed wider), because a pin drawn silently off-screen reads as a button that did nothing; the **initial framing** still belongs to `markers` alone), `fullscreenControl?` (default `false`; a button beside it that blows the map up to fill the viewport, replaced by a close button while open. A **CSS overlay** (`position: fixed`, z-index 1100, page scroll locked through `useScrollLock`), _not_ the Fullscreen API - `requestFullscreen` on an element is still unsupported on iPhone Safari, where the button would simply be dead. `Escape` closes an open marker card first, then leaves fullscreen; entering or leaving re-fits the camera to `markers`, since the viewport changed size), `locateIcon?` / `fullscreenIcon?` / `closeIcon?` (the three glyphs, defaulting to `/icons/location-arrow.svg`, `/icons/fullscreen.svg`, `/icons/close.svg` - the paths every app here keeps them at), `height?` (default `420`), `maxFitZoom?` (deepest zoom the initial framing may pick, default `13` - a single pin otherwise opens on rooftops), `renderPopup?(marker, close)` (the card a marker opens; **omit it and the pins are not interactive** - they render as labelled images rather than buttons, which is what a single-pin "here we are" map wants), `popupWidth?` (default `228`), `tileUrl?` (which basemap to paint, as a `{z}/{x}/{y}` template - defaults to OSM's standard tiles, what this drew unconditionally before. A hosted provider's key rides in it as a query parameter (`…/{z}/{x}/{y}.png?key=…`) and survives, since only the known placeholders are substituted - such a key is fetched from the visitor's browser and is public by construction, so restrict it by origin at the provider. ⚠ **`labels.attribution` and `attributionUrl` must move with it**: every provider requires its own credit, so take all three off one `Basemap` record rather than pairing a new URL with the old string), `attributionUrl?` (where that credit links, when the provider asks for a linked one. Omitted, the credit renders as **plain text**; it is never defaulted to OpenStreetMap's copyright page, which is what this hard-coded for every provider alike until a MapTiler credit ended up naming one party and linking another), `tileFilter?` (`'grade'` | `'none'` - how those tiles are graded once drawn; leave it with the URL, since a pale style under the default `'grade'` washes out and a dark one is inverted back to light), `className?`. ⚠ **Which basemap is a setting; what the basemap draws is not.** These are raster tiles - roads, labels and building footprints are baked into each PNG before it arrives - so there is no prop, and can be no prop, that turns houses off. The routes to a real per-layer switch are to author a style in a tile provider's own editor with the layer deleted and point `tileUrl` at the raster endpoint it publishes, or to replace this renderer with a vector one, which is a different component rather than a flag. Generic over the marker type, so a consumer's own marker fields reach `renderPopup` typed. Draws **OpenStreetMap raster tiles into its own DOM** (no API key, no `leaflet`) - which is the point: the keyless Google embed this replaced was a cross-origin iframe whose single pin could never be given a mark of its own. ⚠ It fetches `tile.openstreetmap.org` **from the visitor's browser**, and the OSM usage policy's attribution is rendered in the corner - leave it. ⚠ **The wheel is cooperative**: a bare scroll belongs to the page and raises a "use Ctrl + scroll" scrim; zooming takes `Ctrl`/`⌘` + wheel (also how a trackpad pinch reports), a two-finger pinch, the buttons or the keyboard - don't "fix" it back to plain wheel zoom. Markers sharing a coordinate are fanned out in screen pixels. The projection is `@repo/ui/core-elements/mercator` (React-free, also imported by `apps/animals`' CMS map picker - one Web Mercator, so a pin cannot land a pixel off the tile beneath it) and **everything it draws is `core-elements/osm-map-chrome`** (see the row below), which the CMS picker renders too - so the two maps are one design. Also exports `OsmMapMarker`, `OsmMapLabels`, `OsmMapProps`. Import: `@repo/ui/core-elements/osm-map`. |
| The furniture of an OSM map surface     | `osm-map-chrome`        | Not one component but the parts **both** OSM maps in the monorepo are built from - `OsmMap` above and `apps/animals`' CMS `MapPicker`, which are deliberately separate components (one _reads_ a marker set, the other _writes_ a coordinate) that must nevertheless look identical. Components: `OsmTileLayer({tiles, filter?})` (the tile layer, tiles as `tilesFor` returns them; `filter` is the basemap's own - `'grade'` wears the treatment written for OSM's standard cartography, `'none'` draws them untouched in **both** themes, which is what a style that already arrives pale or dark needs), `OsmZoomControl({onZoomIn, onZoomOut, zoomInDisabled?, zoomOutDisabled?, zoomInLabel, zoomOutLabel})` (the stacked +/− control, **bottom-right**), `OsmAttribution({label, href?})` (the tile provider's credit, **bottom-left** - required by every one of them, leave it. ⚠ `href` is the **basemap's own** and is optional: this used to anchor every credit to OpenStreetMap's copyright page whatever the label said, so a "© MapTiler" named one party and linked another. With no href the credit renders as plain text - an unlinked credit is a smaller failure than a misdirected one), `OsmGestureHint({text})` (the scrim a bare wheel raises), `OsmUserMarker({left, top, label?})` (the "you are here" teardrop; `label` defaults to English). Constants: `OSM_TILE_URL` (⚠ a `{z}/{x}/{y}` **template**, not the host prefix it used to be, and only a default now - both maps take a `tileUrl`; the styles and their required credits live in `core-elements/basemaps`), `OSM_COPYRIGHT_URL`, `OSM_CHROME_CLASS` / `OSM_MARKER_CLASS` / `OSM_USER_MARKER_CLASS` (hit-test hooks - a viewport's pointer handler asks `closest()` whether a press was the chrome's), `OSM_MARKER_SIZE` (34) / `OSM_MARKER_TIP_HEIGHT`, `OSM_ZOOM_WHEEL_STEP`, `OSM_HINT_MS`, `OSM_DRAG_THRESHOLD`, `OSM_GEOLOCATION_OPTIONS`. ⚠ **A marker is anchored by its tip, not its box**: the shared class rotates the square 45° **about its centre** and offsets it `-120.7%`, so `left`/`top` are the coordinate exactly - anchoring a pin any other way (a `transform-origin` of its own, a `-100%` offset) draws it half a pin off the coordinate it reports. Import: `@repo/ui/core-elements/osm-map-chrome`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| The basemaps a map may be painted from  | `basemaps`              | The tile URL, the required attribution **and where that attribution links**, and the grading of each style, kept **together** in one record - a `Basemap` (`{tileUrl, attribution, attributionUrl?, filter}`). Exports `BASEMAPS` (the keyless styles: `osm`, `carto-light`, `carto-dark`, `carto-voyager` - a provider needing an API key belongs behind `custom`, where the key lives in a setting rather than in the repository), `DEFAULT_BASEMAP`, `OSM_TILE_URL`, `OSM_ATTRIBUTION`, `OSM_COPYRIGHT_URL`, `resolveBasemap(style, custom?)` (`custom` being `{tileUrl?, attribution?, attributionUrl?, filter?}` - an options object rather than four positionals, since the fourth was already a stretch) and the `Basemap` / `BasemapId` / `BasemapFilter` / `CustomBasemap` types. ⚠ **The credit is not decoration and not a translation**: each provider requires a specific string and it changes with the URL, so a CARTO basemap credited '© OpenStreetMap contributors' is under-credited - a licence problem, not a copy problem. That is why the two travel as one record, and why an app renders `attribution` from the basemap it chose rather than from its own message catalogue. ⚠ **`attributionUrl` travels with them for the same reason, and is optional**: most providers want the credit to _link_ back, and `OsmAttribution` used to hard-code OpenStreetMap's copyright page for all of them - naming one party and linking another. Absent, the credit is plain text; `resolveBasemap` only defaults it to `OSM_COPYRIGHT_URL` in the one case where the credit _string_ is also blank and the OSM credit is therefore what is on screen. ⚠ **`filter: 'none'` also disables the dark-theme inversion**, so a light style stays light on a dark page - deliberate, and the choice of whoever picks the style. ⚠ **A raster style's contents still cannot be toggled** - the route to a real per-layer choice is `custom` pointed at a style authored in a provider's own editor with the layer deleted (a self-hosted `tileserver-gl` Helm chart in `packages/charts` was the other route and was dropped as more machinery for the same result; the trade it bought was no per-tile quota, and this renderer fetches 12-20 PNGs per map view). Deliberately React-free, like `mercator` and `breakpoints`. Import: `@repo/ui/core-elements/basemaps`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Social-media icon link row              | `SocialLinks`           | `links: SocialLink[]` (`{platform, url}[]` - the shape of a tenant's `social_links`), `size?` (px, default 40), `color?`, `className?`. Self-contained inline brand glyphs (instagram/facebook/x/tiktok/youtube/linkedin/whatsapp + globe fallback, alias-folded); unknown platform → globe. Renders nothing for an empty list. Also exports `SocialLink`. Import: `@repo/ui/core-elements/social-links`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Data chart (line / pie / radar)         | `Chart`                 | `type: 'line' \| 'pie' \| 'radar'`, `labels: string[]` (shared categories), `series: ChartSeries[]` (each `{ label, data: number[], color? }`; `pie` plots only the first series, one palette color per slice), `height?` (px, default `280`), `hideLegend?`, `ariaLabel?` + UIComponentProps. Generic Chart.js wrapper (chart.js + react-chartjs-2); tick/grid/legend colors track the theme via `--foreground`. Sizes to its container - set `height`. Also exports `ChartType`, `ChartSeries`. Import: `@repo/ui/core-elements/chart`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Crop rectangle over a video             | `VideoCropper`          | `src` (video URL - object-URL, blob-URL or same-origin path), `onChange(rect: CropRect, natural: VideoNaturalSize)` (fires on every drag/preset change; `rect` is in **source-video pixels** with even sides, ready for FFmpeg `crop=W:H:X:Y`), `initialValue?: CropRect \| null` (read **once**, when the intrinsic size lands - the component owns the rectangle after that, so a parent re-render never fights a drag in progress), `presets?: CropAspectPreset[]` (default `DEFAULT_CROP_ASPECT_PRESETS`: Free / 1:1 / 9:16 / 16:9 / 4:5 - a locked `ratio` constrains dragging, `null` is free-form), `showPresets?` (default `true`), `showScrub?` (default `true` - a frame-scrub slider under the preview), `maxPreviewHeight?` (default `'42vh'`), `labels?: VideoCropperLabels` (`aspect`, `scrub`, `selection`), `className?`. Dims everything outside the selection, draws rule-of-thirds guides, and prints the size/offset readout. **Strings default to English** - a localised app passes `labels` (and a `presets` copy with a translated "Free" label). Also exports `CropRect`, `VideoNaturalSize`, `CropAspectPreset`, `VideoCropperLabels`, `DEFAULT_CROP_ASPECT_PRESETS`. Import: `@repo/ui/core-elements/video-cropper`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Trim range on a video timeline          | `VideoTrimmer`          | `src` (video URL), `onChange(range: TrimRange)` (fires on every handle drag; `{ start, end }` in seconds), `duration?` (known length, used until the media's own metadata loads), `initialValue?: TrimRange \| null` (read **once**, like `VideoCropper`'s; defaults to the whole clip), `frameCount?` (filmstrip thumbnails, default `8`), `maxPreviewHeight?` (default `'32vh'`), `labels?: VideoTrimmerLabels` (`play`, `pause`, `start`, `end`, `selected`), `className?`. Renders a seeking preview above a thumbnail strip with two drag handles; the preview follows whichever handle is being dragged and the play button auditions just the selection. Frames are extracted client-side via a detached `<video>` + `<canvas>` - a **cross-origin** `src` taints the canvas, so the strip silently stays empty (route remote media through a same-origin path). Minimum selection 0.5 s. **Strings default to English** - a localised app passes `labels`. Also exports `TrimRange`, `VideoTrimmerLabels`, `formatTimecode(seconds)`. Import: `@repo/ui/core-elements/video-trimmer`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

### Hardware drawings (`packages/ui/src/hardware/`)

**The parts every pictorial hardware figure in the monorepo is drawn from** - a
breadboard, a Raspberry Pi Pico and the discrete components that go on it,
rendered as the objects actually look rather than as schematic symbols. Built
for `apps/help`'s Hardware build sheets, where each figure carries **both** a
schematic and one of these (see `apps/help/CLAUDE.md` → Hardware): a schematic
states the topology, a pictorial states the build - which hole, which way round
the band faces, what the thing in your hand looks like.

⚠ **These are `<g>` fragments, not standalone `<svg>`s.** They compose into one
`PictorialFigure` canvas and are positioned by **breadboard hole**, never by
pixel: `hole(12, "b")` is column 12, row b, and every part takes those points and
works out its own body, legs and labels. That is the whole design - moving a
component is changing a column number. (The hand-placed schematic figures beside
them are the counter-example: moving one symbol there means re-deriving a dozen
`d=` strings.)

⚠ **Three physical rules the geometry enforces, which explain layouts that
otherwise look arbitrary.** A **column is a node** (five holes on one side of the
ravine are one electrical point), so two legs connect by sharing a column and no
part can put both legs in one column. The **Pico occupies a node per column and
covers four rows** - its pins fill row h and row c and its body lies over d, e, f
and g, leaving only i/j and b/a reachable, which is what `footprint.tap()`
returns. And a **rail has no hole every sixth column**, where the moulding's
segment gap falls (`railHasHole`).

| Use case                            | Component             | Key props                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ----------------------------------- | --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The drawing surface + the board     | `breadboard`          | `PictorialFigure` (`width`, `height`, `label` - the long description is **required**, a wiring drawing is opaque to a screen reader without one - `children`, `className`) is the `<svg>` a pictorial figure opens with; it keeps its intrinsic width and expects to sit in a horizontally scrolling box, since squeezing a breadboard until the hole grid collapses is worse than panning it. `Breadboard` (`layout`, `markings?` default `true`, `markingInterval?` default `5`) draws one board's plastic, ravine, hole grid, red/blue rail stripes and the printed column numbers / row letters. Both are plain **server** components - the animations are CSS, so a documentation page stays server-rendered. Import: `@repo/ui/hardware/breadboard`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| The coordinate system               | `breadboard-geometry` | Deliberately **React-free**, like `core-elements/mercator` and `core-elements/breakpoints`. `breadboardLayout({columns, x?, y?})` → a `BreadboardLayout` carrying `hole(column, row)`, `columnX`, `rowY`, `width`/`height` and `ravine`; the origin lives on the object rather than in a module variable, so one figure can hold two boards. `picoFootprint({layout, column, topRow?, bottomRow?})` → a `PicoFootprint` with `pin(numberOrName)`, `pinHole`, **`tap(pin, row?)`** (the free hole a wire to that pin actually plugs into - you cannot wire to the pin itself, it is already in that hole) and `freeRows`. ⚠ `topRow`/`bottomRow` must stay **seven pitches** apart (`h`/`c`, `j`/`e`, `f`/`a`), the Pico's real 0.7" pin spacing, or the drawn body will not line up with its pins. Also exports `HOLE_PITCH`, `HOLE_RADIUS`, `TERMINAL_ROWS`, `RAIL_ROWS`, `railHasHole`, `PICO_PIN_NAMES` (all 40, in pin order), `picoPinKind`, `PICO_COLUMNS`, **`resistorBands(ohms)`** (the real IEC 60062 four-band colour code) and `formatOhms`, plus `midpoint`/`distance`/`angleDegrees` and the `Point` / `BreadboardRow` / `BreadboardLayout` / `PicoFootprint` / `PicoPinKind` types. Import: `@repo/ui/hardware/breadboard-geometry`. |
| Raspberry Pi Pico                   | `PicoBoard`           | `footprint` (from `picoFootprint`), `highlight?: (number \| string)[]` (pins to ring and name - **pass the number for `GND`**, which appears eight times and by name resolves to pin 3), `labels?: 'used' \| 'all' \| 'none'` (default `'used'`), `labelPlacement?: 'inside' \| 'outside'`, `labelStandoff?`. Draws the real board: green PCB, castellated edges, gold pads, micro-USB shield, BOOTSEL, the RP2040 under the raspberry, the GP25 LED, the debug pads. ⚠ **`labelPlacement` defaults from `labels`, and the reason matters**: on a breadboard everything outside the pin row is _wiring_, so the holes an outward chip would cover are exactly the ones the figure's jumpers plug into - a wiring diagram therefore prints its few names on the PCB itself (`'used'` → `inside`, silkscreen-style) while a reference card, which has no wires to fight, spreads all forty outward (`'all'` → `outside`). Also exports `PicoLabelMode`. Import: `@repo/ui/hardware/pico`.                                                                                                                                                                                                                                                             |
| Jumper wires, leads, callouts       | `wire`                | `Wire` (`from`, `to`, `color?` - a `WIRE_COLORS` name or any CSS colour - `bow?` default `0.09`, `bowDirection?`, `thickness?`, **`flow?`** (marching dashes showing which way current travels; use it where direction is the lesson - supply rails, the ground return - and leave it off signal wires, where a dozen crawling dashes just make the figure noisy), `ends?`). `Lead` is the same geometry drawn thin and in bare metal for a component's own leg, so the eye reads "part" rather than "another jumper you have to add". `Callout` (`at`, `to`, `anchor?`) hangs a value off a leader line - a pictorial figure has nowhere to write a value _on_ a part, since a 5 mm LED is 9 px across at this scale. Also exports `WIRE_COLORS`, `WireColor`. Import: `@repo/ui/hardware/wire`.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| LEDs                                | `led`                 | `Led` (`anode`, `cathode`, `color?`, `state?: LedState`, `radius?`, `id` - **must be unique in the figure**, it names the glow gradient - `polarity?`) draws the domed lens, the flange and **the flat on the cathode side**, which is the point of drawing it pictorially at all: "the short leg, on the flattened side, is the cathode" is a sentence a triangle cannot say. `RgbLed` (`red`, `cathode`, `green`, `blue`, `state?` per die, `radius?`, `id`) takes four named holes because leg order on the part is **red · cathode · green · blue**, not the R/G/B-plus-common a reader would assume; the lens is drawn as three sectors so it is obvious one package holds three dice sharing one return. `LedState` is `'off' \| 'on' \| 'pulse' \| 'flicker'` - `flicker` is the firmware's flame, deliberately uneven, since even steps read as a loose connection. Import: `@repo/ui/hardware/led`.                                                                                                                                                                                                                                                                                                                                        |
| Resistor, diode, transistor, buzzer | `discretes`           | Each takes the holes its legs go into and works out its own body position and angle, so a figure never places a body by hand. `Resistor` (`from`, `to`, `ohms`, `showValue?`, `labelSide?`) paints the **real colour code** from `resistorBands` - sorting a bag of resistors means matching bands, and 150 Ω (brown-green-brown) against 68 Ω (blue-grey-black) is a comparison you can make straight off the drawing. `Diode` (`anode`, `cathode`, `label?`, `body?: 'black' \| 'glass'`, `labelSide?`) leads with the cathode stripe, the only marking on the part. `Transistor` (`emitter`, `base`, `collector`, `label?`, `designator?`) draws the TO-92 standing clear of its own leg holes and labels E · B · C, because that order is **not** universal - a 2N2222 and a BC547 have theirs swapped. `Buzzer` (`plus`, `minus`, `label?`, `sounding?`, `radius?`) radiates rings when `sounding`. ⚠ A printed value is offset along the body's **normal**, so a part straddling the ravine puts its value beside itself rather than on top of itself; a diode's "band" hint goes **past the cathode along the axis**, because sideways is the lane the wire feeding that column runs in. Import: `@repo/ui/hardware/discretes`.              |
| Cell holder and slide switch        | `power-parts`         | The two parts that do **not** live on the breadboard, so they take a plain `x`/`y` and expose their lead ends as anchors for a `Wire`. `BatteryPack` (`x`, `y`, `cells?` default `4`, `label?`, `sublabel?`) + `batteryPackLeads({x, y, cells})` → `{positive, negative}`. `SlideSwitch` (`x`, `y`, `on?`, `label?`) + `slideSwitchTerminals({x, y})` → `{left, right}`; the actuator is drawn thrown to whichever side `on` says, which is more useful than it sounds for a build whose switch is in the **negative** lead. Import: `@repo/ui/hardware/power-parts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

**Styling.** `hardware.css` is deliberately **one sheet for the whole family**,
not one per component (the documented exception to the rule below): these are not
ten independent widgets but one _drawing style_ - a resistor's body colour and
the breadboard's substrate have to be chosen against each other, and the glow,
the current-flow dashes and the sound rings are the same three keyframes reused
by several parts. Its colour policy is worth knowing before changing a value:
part colours are **literal** and do not re-theme (a red wire that turned grey in
dark mode would teach the reader the wrong thing), while the **substrate and the
text** take the page's foreground and flip with `[data-theme="dark"]`. Text
printed on a part uses the `hw-silk` class rather than a `fill` attribute,
because in SVG a CSS declaration beats a presentation attribute and the sheet's
own `.hw-fig text { fill }` would otherwise win. Under
`prefers-reduced-motion: reduce` the animations stop at their **bright** state
rather than disappearing, so the drawing loses its motion, not its meaning.

### Other UI exports (`packages/ui/src/`)

| Component | Purpose |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ---------- | ---------- | ---------- | --------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `useScrollLock` | `useScrollLock(active = true)` - freezes the page behind an overlay (modal, fullscreen viewer, drawer) while `active`. ⚠ **It locks `<html>` as well as `<body>`, which is the point**: every app in `apps/` sets `html, body { overflow-x: hidden }`, and once the root element's overflow is not `visible` the spec stops propagating the body's overflow to the viewport - so the `document.body.style.overflow = 'hidden'` each overlay used to hand-roll was a no-op in all of them and the page went on scrolling behind the dialog. Never "simplify" it back to the body alone. **Reference-counted**, so a dialog opened over a drawer restores scrolling only when the last holder releases (and React's dev double-effects balance out). It compensates the hidden scrollbar with padding on `<body>` so the page doesn't jump sideways, and publishes that width as `--ui-scroll-lock-gutter` on `<html>` (`0px` while unlocked) for anything an app pins outside the body flow. It deliberately does **not** touch `touch-action`: an overlay that wants a pinch (a photo viewer) sets `touch-action: none` on itself, which would kill finger-scrolling inside a modal's own scrollable panel if it were global. **Every overlay in the monorepo goes through it** - `ConfirmationModal`, `Drawer`, `OsmMap`'s fullscreen, and `ImageGallery`'s - and a new one must too rather than hand-rolling a fourth copy, which is how the three that existed each ended up locking the wrong element. Import: `@repo/ui/core-elements/use-scroll-lock`. |
| `scrollToElement` / `ScrollTo` | **Every scroll in `apps/` goes through this module** - `scrollToElement(target, opts?)` brings an element into view, `scrollWindowTo(top, opts?)` / `scrollToTop(opts?)` move the window itself, and `<ScrollTo target>` is the declarative form for a page that must open somewhere other than its own top. ⚠ **The reason not to keep calling `scrollIntoView` directly is `prefers-reduced-motion`**: a smooth scroll is animation a reader may have asked not to be shown, and every one of the six hand-rolled copies this replaced (the booking page, both video-downloader duplicate/page-turn scrolls, the menu-ingredients editor, cinelog's pagination and its scroll restore) forgot to check. These consult it once and downgrade to an instant jump - the reader still arrives, without the travel. `target: ScrollTarget` is a **CSS selector** (`"#booking-heading"`, `'[data-video-uuid="…"]'`), an `Element`, or a ref - `null`/`undefined` are in the type on purpose, so `ref.current` or a lookup that found nothing needs no guard and simply returns `false`. Options are `block?` (default `"start"`), `inline?` (default `"nearest"`) and `smooth?` (default `true`); there is deliberately **no offset argument** - the space a fixed navbar needs is the target's own `scroll-margin-top` (`styles={{ scrollMarginTop: "var(--ui-navbar-height, 57px)" }}`), which the element knows and a distant caller does not. Pass `smooth: false` when restoring a saved position rather than travelling somewhere new, or the returning page looks like it scrolled itself. `<ScrollTo>` renders nothing, waits **one frame** (a smooth scroll aimed at a pre-layout position lands short of a hero that hasn't been measured yet) and by default **will not move a page that is not already at the top** (`onlyWhenAtTop`, 4px of slack) - a restored position is where the reader left off, and hijacking it throws away their place in a form; pass `onlyWhenAtTop={false}` only when the scroll must win regardless. Also exports `resolveScrollTarget`, `prefersReducedMotion`, and the `ScrollTarget` / `ScrollToOptions` / `ScrollToProps` types. Import: `@repo/ui/core-elements/scroll-to`. |
| `ThemeProvider` | Wraps app; provides `--accent`, `--background`, etc. CSS vars |
| `PaletteProvider` | Overrides accent palette |
| `ThemeSwitch` | Light/dark toggle button |
| `Hero` | Full-width hero section component. `parallax?` (default `true`) drifts the background via `ParallaxLayer`; pass `false` for a static background. `layout?: HeroLayout` (`'default'` \| `'none'` \| `'profile'`, default `'default'`) picks how the logo and slogan sit over the background: `'default'` centres both over it, `'none'` drops the logo entirely and centres only the text (slogan/subline) and any `actions` over the background (for a video that already carries the branding, or a text-only hero), `'profile'` puts the logo in an elevated circle straddling the hero's bottom edge (half in, half out, like a profile picture on a cover photo) with the slogan centred in the space above. `'profile'` needs only a `logoImage` - with `logoBackground: 'none'` the bare logo straddles the edge instead of a badge - and falls back to `'default'` with no `logoImage`. It paints the circle with `var(--page-background, var(--background))` (the app resolves `--page-background` per theme), and hangs the disc off a wrapper whose bottom margin reserves the overhang - so the hero must not be placed inside an `overflow: hidden` box. `contentScale?` (default `1`) multiplies the logo/slogan/profile-circle sizes only (not the hero box) - pass a smaller value like `0.5` to shrink the overlaid content for a constrained preview. `profileLogoScale?` (default `1`, clamped `[0.1, 1]`) is the drawn size of the logo inside the badge as a fraction of it: `1` is the edge-to-edge `cover` fill, a smaller value shrinks the logo about the badge centre leaving a ring of the badge's page-background around it. `profileBackgroundScale?` (default `1`, clamped `[0.1, 1]`) shrinks the whole badge (shape + logo) about its centre, independent of `profileLogoScale`. `logoBackground?: HeroLogoBackground` picks the badge shape - the geometric shapes plus `'logo'`, which clips the plate to the logo's own silhouette via a CSS mask on its alpha (needs a transparent logo). The badge's elevation is a `drop-shadow` filter (`HERO_BADGE_SHADOW`), not a `box-shadow`, so it traces the clipped/masked silhouette of every shape. Four props compose the text over the background, all defaulting to the original centred look: `subline?` (a quieter supporting line under the slogan, so the two read as headline + subtitle rather than two equal sentences), `align?: 'center' \| 'start'` (default `'center'`; `'start'` left-aligns the text and caps its measure at 20ch/42ch - the logo stays centred), `actions?: ReactNode` (a CTA row under the text, following `align` - the caller supplies its own `Button`/`LinkButton` so this component stays free of routing and i18n), and `scrim?` (0-1 flat black over the whole frame, under the content; the built-in gradient only darkens the bottom ~45%, so text higher up otherwise leans on a text-shadow - which the slogan drops automatically once a scrim is present). The slogan takes `var(--font-display, inherit)`, so an app that publishes that variable gets its display face in the hero. In the `default` layout the badge draws smaller than the `profile` disc and the whole logo+text overlay is inset from the top by `--ui-navbar-height`, so a centred logo never tucks under the fixed navbar; in `profile` the text/CTA clears the disc by 24px. The `logo`-shape mask is routed through `heroLogoMaskUrl` so it stays same-origin - a cross-origin `mask-image` resolves to an _empty_ mask in Chromium/WebKit (the plate then vanishes in production even though the logo still shows). Three props own the **dark overlay** between the background and the content: `overlayStyle?: HeroOverlayStyle` (`'none'` \| `'full'` \| `'bottom'` \| `'top'` \| `'both'` \| `'vignette'`, default `'bottom'`), `overlayOpacity?` (0-1, default `0.75` - how _dark_ the darkest part gets) and `overlayExtent?` (0-100, default `DEFAULT_HERO_OVERLAY_EXTENT` = `50` - how _far_ the gradient reaches across the frame, a taller/shorter dark band, independent of opacity) - the defaults reproduce the gradient this component used to hard-code, so an existing hero is unchanged. Each style keeps its historical fade stop as the value at extent `50`; `full` is a flat tint so `overlayExtent` does nothing to it. `overlayOpacity` of `0` (or `'none'`) draws no overlay at all; resolve the trio with the exported `heroOverlayBackground(style, opacity, extent?)` rather than hand-writing a second gradient, which is how the landing hero and the website's `ItemHeroVideo` drift apart. `scrim` is unrelated and still composes underneath. Three props add an opt-in **bottom shape divider** that softens the hard horizontal seam where the hero meets the page below by cutting a **real transparent notch** out of the hero's edge (masking the whole hero - video, black backing and overlay - not painting a band over it), so the page background and its fixed logo watermark show through the notch and the video appears to dissolve into the page: `bottomDivider?` is one of the named `ShapeDividerMask` shapes (`'wave'` | `'scallop'` | `'zigzag'` | `'spikes'` | `'arches'` | `'slant'` | `'inverted-slant'`) or `'none'`/`null` for the historical hard edge, `bottomDividerEdge?` (`'top'` | `'bottom'`, default `'bottom'`) picks which edge the notch is cut into, and `bottomDividerHeight?` sets the band height (default `clamp(30px, 5vw, 64px)`, the notch depth growing with it). The mask is resolved by the shared **`shapeDividerMaskStyle`** from `@repo/ui/shape-divider`, so the hero and any standalone `ShapeDivider` cut the same shape. `bottomDividerElevation?` (default `0`, scale 1-24 mirroring `@repo/ui-native`'s `Box`) lifts the divider edge off the page with a `drop-shadow` that traces the notch silhouette - the hero is wrapped in an outer element so the filter reads the already-masked shape (a `filter` on the masked box itself is applied _before_ the mask and would be clipped away in the notch); `drop-shadow` not `box-shadow` so it follows the shape, not the rectangle. **A bottom divider also lifts the `profile` disc** by `shapeDividerEdgeInset(bottomDivider, bottomDividerHeight)` - the shape's notch depth around the middle of the edge, where the disc sits - because several shapes make the very edge the disc straddles largely transparent, so a disc pinned to the nominal edge hangs its inner half over a hole and reads as having slipped below the hero. The text/CTA clearance rises with it (keeping its 24px gap) and the wrapper's reserved overhang shrinks by the same amount (keeping its 16px), so the lift moves the mark without opening a gap above the next block. Three props add an **outline frame** around the slogan/subline for section/page headings (never the landing hero): `frame?` (default `false`) wraps the text in a thin white outline that hugs the heading, over a **frosted-glass backdrop** (`backdrop-filter: blur(8px)`, the one blur radius the design system uses - `Box`'s `translucent`, the translucent `Navbar`/`Badge` - so a framed heading reads as a pane of glass over the video rather than a bare rule; with a `frameImage` the glass **continues up into the cradle**, a second pane masked to the area the shoulders enclose - ⚠ a _sibling_ of the frame box, never a child, since an element with a `backdrop-filter` is a backdrop root and a nested pane samples only what that ancestor painted, which above its own top edge is nothing), `frameImage?` (a brandmark URL) sits in a white circle straddling the top border - half above, half below - and `frameImageAlt?` is its alt text; with no `frameImage` the frame is a bare outline. The frame is the exported **`HeroTextFrame`** component (`children`, `image?`, `imageAlt?`, `scale?`), reused by the website's `ItemHeroVideo` so both heroes frame text identically. The cradle it hangs the brandmark in is itself the exported **`BrandmarkCradle`** (`image`, `imageAlt?`, `size?` = `HERO_FRAME_BADGE_SIZE`, `color?`, `circleBackground?`, `fill?`, `overhang?`, `strokeWidth?` = `HERO_CRADLE_STROKE`) - the gapped rule, the two rising shoulders and the disc, absolutely positioned against the **top edge** of a `position: relative` parent that draws no top border of its own (these flanks are it) and reserves `size * 0.85 + 8px` above itself, or the disc hangs over whatever is there. `fill` (default `null`) paints the area the shoulders enclose, so the parent's surface reads as swelling up into the cradle instead of the page showing through a notch in its edge - pass the parent's own background; a frame floating over a video leaves it off. Split out so a second surface can cradle the same mark without a second copy of the arc paths: `apps/website`'s **footer** draws one on its top edge, in the footer's own border colour with a `var(--page-background)` disc instead of the hero's white-over-video. ⚠ **The shoulders' weight is a viewport `clamp()`, not a number** (`HERO_CRADLE_STROKE` = `clamp(2px, calc(0.5vw - 1px), 5px)`, the `strokeWidth` default): they carry `vector-effect: non-scaling-stroke`, so a fixed weight stays the same screen pixels while the badge's own `clamp()` shrinks the cradle under it - the 5px that reads right against a 50px badge on a desktop comes out heavy and blunt against a 36px one on a phone. It is applied as the CSS `stroke-width` **property**, never the SVG attribute of that name, which takes a plain length and silently drops the `clamp()`; `HeroTextFrame` scales it by its `scale` for a shrunk preview, as it does the badge and padding. Also exports `HeroLayout`, `HeroLogoBackground`, `HeroOverlayStyle`, `heroLogoBackgroundStyle`, `heroLogoMaskStyle`, `heroLogoMaskUrl`, `heroOverlayBackground`, `DEFAULT_HERO_OVERLAY_EXTENT`, `HeroTextFrame`, `BrandmarkCradle`, `HERO_FRAME_BADGE_SIZE`, `HERO_CRADLE_STROKE`, `HERO_BADGE_SHADOW` and `HERO_PROFILE_LOGO_SIZE`. |
| `ShapeDivider` | Wraps `children` in a `position: relative; overflow: hidden` box and cuts a shape-divider **notch** out of one edge, so whatever is painted behind the box (page background, watermark) shows through the notch - softening the hard seam between a hero image/video and the page. `children`, `mask?: ShapeDividerMask \| null` (`'wave'` \| `'scallop'` \| `'zigzag'` \| `'spikes'` \| `'arches'` \| `'slant'` \| `'inverted-slant'` \| `'brandmark'`; omit/`null` renders `children` untouched, so it binds straight to a nullable "none" setting). The `scallop` half-circles are **concave bites cut up into the edge** (inverse of downward-hanging lobes). `slant` is a single straight diagonal rising left to right; `inverted-slant` is the same diagonal mirrored, falling left to right (both stretch across the edge rather than tiling). `brandmark` tiles a caller-supplied image along the edge as a stamped lace border (`repeat-x` at the band height) - it needs `brandmarkUrl` and that URL **must be same-origin** (route remote assets through `/_next/image`), or the mask resolves empty in Chromium/WebKit and the notch silently vanishes; a `'brandmark'` mask with no `brandmarkUrl` renders `children` untouched, and the image is used as-authored for both edges (only the band position follows `edge`). `brandmarkUrl?: string`, `edge?: 'top' \| 'bottom'` (default `'bottom'`), `size?` (band thickness, default `clamp(30px, 5vw, 64px)`), `elevation?` (a `drop-shadow` that traces the notch contour so the shaped edge lifts off the page - mirrors `@repo/ui-native`'s `Box` elevation scale 1-24; `drop-shadow` not `box-shadow`, since the notch would clip a box-shadow flat), `className?`, `style?`. The mask cuts a **real** hole (it clips every descendant), so only use it where something meaningful sits behind the box, and keep anything that must escape the notch outside the wrapper. Also exports the `shapeDividerMaskStyle(mask, edge, size, brandmarkUrl?)` helper (the CSS `mask` props, for a box you already own the styles of - `Hero` spreads it onto its own box), `shapeDividerElevationFilter(elevation)` (the `drop-shadow` filter string for that elevation), `shapeDividerEdgeInset(mask, size)` (how far that shape's notch pulls the box's _effective_ edge inward, as a `calc()` length - the per-shape cut depth around the MIDDLE of the edge, measured off the paths themselves (which is why `wave`, shallowest dead centre, insets far less than its whole-edge average would suggest), so anything centred on the notched edge, e.g. `Hero`'s `profile` disc, can move with it instead of hanging over the hole), `SHAPE_DIVIDER_MASKS` (the shapes in picker order), and the `ShapeDividerMask` / `ShapeDividerEdge` / `ShapeDividerProps` types. Import: `@repo/ui/shape-divider`. |
| `ParallaxLayer` | Scroll layer that rises a hero background more slowly than the page. Fills a positioned, `overflow: hidden` parent and renders its child in a box overscanned by `1 - speed` above it, sliding it down as the hero passes the top of the viewport. `children` (the background - an `<img>`, `HeroVideo`, etc.), `speed?` (default `0.5` = half the page's scroll speed; `1` disables the effect, `0` pins the background - the child is cropped by `1 - speed` to cover the drift), `disabled?` (default `false` - renders the child untouched and uncropped). Anchors to the viewport top, so it assumes the hero starts at the top of its page; one placed lower simply stays still until it gets there. Skips scroll entirely under `prefers-reduced-motion: reduce`. |
| `HeroVideo` | Absolutely-positioned autoplaying video layer for a `position: relative` parent (used by `Hero`, and by the website's product/service detail hero). `url` (required), `playing?` (default `true`; set `false` to pause a background hero while another player - e.g. a fullscreen modal copy - has the user's attention), `controls?` (default `false`; also makes the player interactive - a controlless player sets `pointer-events: none` so it never swallows clicks), `muted?` (default `true` - browsers only autoplay muted video), `loop?` (default `true`), `fit?: 'cover' \| 'contain'` (default `'cover'` crops to fill; `'contain'` letterboxes the whole frame). Defaults reproduce the muted background used by `Hero`. |

### Import path convention

Each core-element is imported by its individual file path (the wildcard export in `packages/ui/package.json` handles resolution automatically - no index file or registration needed):

```tsx
import { Box } from "@repo/ui/core-elements/box";
import { Button } from "@repo/ui/core-elements/button";
import { Grid } from "@repo/ui/core-elements/grid";
// etc.
```

Top-level UI exports use their direct path:

```tsx
import { ThemeProvider } from "@repo/ui/theme-provider";
import { Hero } from "@repo/ui/hero";
```

### Adding or modifying a core element

When creating a new component in `packages/ui/src/core-elements/` or changing the props of an existing one:

1. **No package registration needed** - the `"./*": "./src/*.tsx"` wildcard export covers it automatically.
2. **Always create a companion `.css` file** for the component's styles (see Styling Conventions below).
3. **Update the component table in this file** (`packages/ui/CLAUDE.md`) to reflect the new or changed component and its key props. This must be done in the same task - never leave the table stale.

## Breakpoints

All responsive behaviour must align with the breakpoint scale, the single source of truth for every breakpoint threshold in the monorepo. It lives in `packages/ui/src/core-elements/breakpoints.ts` (a React-free module so build tooling can import it) and is re-exported from `core-elements/utils` for backwards compatibility - import from either `@repo/ui/core-elements/breakpoints` or `@repo/ui/core-elements/utils`:

```
xs:   0 px   (mobile base)
sm: 600 px
md: 900 px
lg: 1200 px
xl: 1536 px
```

- **In TypeScript/TSX** - use the `Breakpoint` type and `BREAKPOINTS` record directly; never hardcode pixel values for breakpoint logic.
- **In CSS** - the scale is compiled to `@custom-media` tokens by
  `scripts/gen-breakpoints-css.ts` → `src/core-elements/breakpoints.generated.css`
  (committed; **never edit by hand** - regenerate with `pnpm --filter @repo/ui gen:breakpoints`).
  This is the **one** generated tokens file for the whole monorepo: every Next.js app's
  `postcss.config.mjs` points `@csstools/postcss-global-data` at it, so the tokens are
  injected (and `postcss-custom-media` resolves them) into **every** CSS file each app
  bundles - **including this package's own `.css` files**. So a `@repo/ui` component's CSS
  may use the tokens directly (e.g. `navbar.css`); they resolve through the consuming
  app's PostCSS. Write the token, never a pixel literal:

```css
@media (--below-sm) { … }   /* below the sm breakpoint (mobile only) */
@media (--sm)       { … }   /* from sm up */
@media (--md)       { … }   /* from md up */
@media (--only-lg)  { … }   /* within the lg band only */
```

`@repo/ui` also ships a reference `postcss.config.mjs` so the package is self-describing;
note it does **not** run during an app build (Next resolves a single config from the app
root), so a `@repo/ui` CSS file's tokens are only guaranteed to resolve in consumers that
run the `postcss-global-data` + `postcss-custom-media` pair - which every app in `apps/` does.

## Styling Conventions

- **Always use a separate `.css` file** for component-specific styles. Never use inline `style={{}}` objects or CSS-in-JS for custom component styles.
- Name the CSS file after the component: `my-component.css` alongside `my-component.tsx`, then `import './my-component.css'` at the top of the component file.
- `UIComponentProps` layout props (`padding`, `gap`, `width`, etc.) and the `styles` escape-hatch prop are the only acceptable inline styles - they are part of the `@repo/ui` design system contract.

### Component-props-first rule

**Static styles on `@repo/ui` components must always be expressed as component props - never in a CSS file.**

| Use component props                                                                                                     | CSS is the only option                                        |
| ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| Layout: `display`, `flexDirection`, `gap`, `alignItems`, `justifyContent`, `flex`, `flexWrap`                           | `:hover`, `:focus`, `:disabled`, `:active` pseudo-class rules |
| Sizing: `width`, `height`, `minWidth`, `maxWidth`, `minHeight`, `maxHeight`                                             | `transition` and `animation`                                  |
| Spacing: `padding`, `paddingX`, `paddingY`, `paddingTop/Bottom/Left/Right`, `margin`, `marginTop`, `marginBottom`, etc. | `transform` (active-state indicators)                         |
| Borders: `border`, `borderRadius`                                                                                       | `@media` queries for breakpoint-specific overrides            |
| Color: `color`, `backgroundColor`                                                                                       | `::before` / `::after` pseudo-elements                        |
| Shadow / elevation: `elevation`, `shadow`                                                                               | Structural selectors (`:nth-child`, etc.)                     |
| Typography: `fontWeight`, `textAlign`, `color`                                                                          |                                                               |

**For CSS properties not covered by `UIComponentProps`** (e.g. `fontSize`, `letterSpacing`, `textTransform`, `opacity`, `lineHeight`, `whiteSpace`, `overflow`, `textOverflow`) use the `styles` escape-hatch prop - **never** put them in a CSS class:

```tsx
// ✗ Wrong - static values in a CSS class
<Typography as="h3" variant="h3" className="my-label">…</Typography>
/* .my-label { font-size: 11px; font-weight: 700; color: #999; letter-spacing: 0.06em; text-transform: uppercase; } */

// ✓ Correct - all expressed as props
<Typography
  as="h3"
  variant="h3"
  color="var(--foreground-muted, #999)"
  fontWeight={700}
  styles={{ fontSize: 11, letterSpacing: '0.06em', textTransform: 'uppercase' }}
>…</Typography>
```

**Rule:** a CSS class attached to a `@repo/ui` component must contain **only** entries from the right column above (pseudo-class rules, transitions/animations, `@media` overrides, pseudo-elements). If a CSS class contains nothing but left-column properties, delete it entirely and use props. Dynamic values driven by React state belong on the prop using a ternary, not in a CSS class.

### `globals.css` - shared utility classes

Each Next.js app in `apps/` has its own `app/globals.css`, which is the single source of truth for styles that recur across more than one component within that app.

**Rules:**

- **Before adding any style to a component CSS file, check whether an equivalent class already exists in `globals.css`.** If it does, use it.
- **If a pattern appears in two or more components, move it to `globals.css`** and delete the duplicates from the component files.
- **Component CSS files are for component-specific overrides only.**
- Never redefine a utility class from `globals.css` inside a component file. Use a scoped override (e.g. `.my-header .section-title { margin-bottom: 0; }`) when the shared default needs a local adjustment.
