# ── Server ─────────────────────────────────────────────────────────────────────
PORT=8000
LOG_LEVEL=info          # trace | debug | info | warn | error

# ── Authentication ─────────────────────────────────────────────────────────────
# API key required on every request via X-API-Key header.
# Leave empty to disable auth (development only).
API_KEY=your-strong-api-key-here

# ── HuggingFace ────────────────────────────────────────────────────────────────
# When using the multi-stage Dockerfile (recommended), HF_TOKEN is only needed
# at build time as a Docker build arg:
#   docker build --build-arg HF_TOKEN=hf_... -t diarization:latest .
# It is NOT required at runtime - the image runs fully offline (HF_HUB_OFFLINE=1).
#
# For local development without the baked image, set HF_TOKEN here so models
# are downloaded on first startup.
# Accept the terms at:
#   https://huggingface.co/pyannote/speaker-diarization-3.1
#   https://huggingface.co/pyannote/segmentation-3.0
# Then generate a token at https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here

# ── Model configuration ────────────────────────────────────────────────────────
# Whisper model size: tiny | base | small | medium | large-v2 | large-v3
# Larger models are more accurate but slower and require more memory.
# Recommended for CPU-only: base (default) or small
WHISPER_MODEL_SIZE=medium

# ── Deployment ─────────────────────────────────────────────────────────────────
DOCKER_REGISTRY=christopherguzman
NAMESPACE=diarization
