apiVersion: v1
kind: Service
metadata:
  name: {{ include "diarization.fullname" . }}
  labels:
    {{- include "diarization.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "diarization.selectorLabels" . | nindent 4 }}
