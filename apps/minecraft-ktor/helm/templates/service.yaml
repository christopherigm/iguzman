apiVersion: v1
kind: Service
metadata:
  name: {{ include "minecraft-ktor.fullname" . }}
  labels:
    {{- include "minecraft-ktor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "minecraft-ktor.selectorLabels" . | nindent 4 }}
