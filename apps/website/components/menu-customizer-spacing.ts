/**
 * The space between the size choice and the add-ons, on every surface where a
 * dish is customised - the item detail page, the catalog card's add-to-cart
 * modal (which the cart also re-opens to edit a line) and the POS till.
 *
 * A step above the rhythm inside the pickers themselves - the size cards' and
 * the add-on rows' own gaps - so that which size and what goes on it read as
 * two separate decisions about the dish: packed to that tighter rhythm, the
 * add-on list reads as a caption under the size row rather than as the next
 * question. It lives here for the same reason the two pickers themselves are
 * shared - three copies of the number could only drift, and the three surfaces
 * are meant to be one control in three shells.
 */
export const MENU_CUSTOMIZER_GAP = 16;
