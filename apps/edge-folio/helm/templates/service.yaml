apiVersion: v1
kind: Service
metadata:
  name: {{ include "edge-folio.fullname" . }}
  labels:
    {{- include "edge-folio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "edge-folio.selectorLabels" . | nindent 4 }}
