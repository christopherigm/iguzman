apiVersion: v1
kind: Service
metadata:
  name: {{ include "edge-folio-api.fullname" . }}
  labels:
    {{- include "edge-folio-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "edge-folio-api.selectorLabels" . | nindent 4 }}
