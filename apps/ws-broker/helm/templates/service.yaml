apiVersion: v1
kind: Service
metadata:
  name: {{ include "ws-broker.fullname" . }}
  labels:
    {{- include "ws-broker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ws-broker.selectorLabels" . | nindent 4 }}
