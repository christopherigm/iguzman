apiVersion: v1
kind: Service
metadata:
  name: {{ include "horoscope.fullname" . }}
  labels:
    {{- include "horoscope.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "horoscope.selectorLabels" . | nindent 4 }}
