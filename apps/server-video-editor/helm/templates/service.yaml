apiVersion: v1
kind: Service
metadata:
  name: {{ include "server-video-editor.fullname" . }}
  labels:
    {{- include "server-video-editor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "server-video-editor.selectorLabels" . | nindent 4 }}
