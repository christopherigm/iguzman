apiVersion: v1
kind: Service
metadata:
  name: {{ include "tanda.fullname" . }}
  labels:
    {{- include "tanda.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tanda.selectorLabels" . | nindent 4 }}
