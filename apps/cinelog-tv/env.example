# Vite exposes only VITE_-prefixed vars to the client bundle.
VITE_API_URL=https://cinelog-api.iguzman.com.mx
# Cinelog web app base, shown on the pairing screen as where to enter the code.
VITE_WEB_URL=https://cinelog.iguzman.com.mx
