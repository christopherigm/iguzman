apiVersion: v1
kind: Service
metadata:
  name: {{ include "cinelog.fullname" . }}
  labels:
    {{- include "cinelog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "cinelog.selectorLabels" . | nindent 4 }}
