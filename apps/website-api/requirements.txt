Django==5.2.11
djangorestframework==3.16.1
djangorestframework_simplejwt==5.5.1
Pillow==11.1.0
# The QR code stored on every order (`orders/services/qr.py`). Pure Python and
# renders through Pillow, which is already here - no native build, no new wheel
# platform to worry about in the image.
qrcode==8.2
whitenoise==6.9.0
gunicorn==23.0.0
psycopg2-binary==2.9.10
# Cloudflare R2 media storage (production only - dev writes to MEDIA_ROOT).
# boto3 is pulled in transitively but pinned explicitly: core/storage.py imports
# botocore directly to classify credential errors for the CMS test button.
django-storages[s3]==1.14.4
boto3==1.43.34
django-redis==5.4.0
django-colorfield==0.11.0
django-cors-headers==4.9.0
webauthn==2.7.1
cryptography==46.0.6
stripe==14.2.0
openai>=1.0.0
requests==2.32.3
python-dotenv==1.1.1
